const {Sequelize, sequelize} = require('../db');

module.exports = {
    Post: {
        Get:(req, res)=>{
            
        },
        Put:(req, res)=>{}
    },
    Todo: {
        Get:(req, res)=>{},
        Put:(req, res)=>{}
    },
    User: {
        Get:(req, res)=>{},
        Put:(req, res)=>{}
    }
}